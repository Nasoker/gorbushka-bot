# Telegram Bot

Telegram бот на Telegraf.js с системой ролей и управлением пользователями.

## Возможности

- 🤖 Telegram бот с системой ролей (админ/модератор/пользователь)
- 👥 Управление пользователями и ролями
- 🗄️ Сохранение данных в SQLite базе данных
- 📋 Админ панель с командами управления

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
```

3. Укажите токен Telegram бота в файле `.env`:
```
BOT_TOKEN=your_telegram_bot_token_here
```

## Запуск

### Обычный режим:
```bash
npm start
```

### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

## Команды бота

### Для всех пользователей:
- `/start` - Начать работу с ботом
- `/help` - Справка по командам

### Для администраторов:
- `/admin` - Админ панель
- `/addModer [ID]` - Добавить модератора
- `/users` - Список всех пользователей

## Система ролей

- **admin** - Полный доступ ко всем функциям
- **moderator** - Доступ к функциям модерации
- **user** - Базовый доступ к боту

Администраторы настраиваются в файле `config/admins.json`.

## Структура проекта

```
src/
├── index.js            # Главный файл приложения
├── bot/
│   ├── bot.js          # Основной класс Telegram бота
│   └── userManager.js  # Менеджер пользователей
└── database/
    └── database.js     # Класс для работы с SQLite
```

## Конфигурация

### Администраторы
Администраторы настраиваются в файле `config/admins.json`:

```json
{
  "admins": [
    {
      "user_id": 123456789,
      "username": "admin_user",
      "first_name": "Admin"
    }
  ]
}
```

### Переменные окружения
Создайте файл `.env`:

```
BOT_TOKEN=your_telegram_bot_token_here
```

## База данных

Бот использует SQLite базу данных для хранения:
- Информации о пользователях
- Ролей пользователей
- Статистики

База данных создается автоматически при первом запуске в файле `./data/bot.db`.

## Логирование

Все операции логируются в консоль с эмодзи для удобства:
- ✅ Успешные операции
- ❌ Ошибки
- 🤖 Действия бота
- 👥 Управление пользователями
- 🗄️ Операции с базой данных

## Разработка

Для разработки используйте режим с автоперезагрузкой:

```bash
npm run dev
```

Этот режим автоматически перезапускает бота при изменении файлов в папке `src/` или `config/`.